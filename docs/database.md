# Database layout (welfare-db)

Static JSON under `public/welfare-db/`:

- `welfare/*.json` — benefit records (arrays), merged at load time
- `tags/tags.json` — tag vocabulary
- `region/region.json` — region list
- `meta/categories.json` — category list

See the repository root `DB 구조 설계.txt` for field shapes and ID rules.
