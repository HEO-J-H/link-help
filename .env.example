# Web Push (must match server VAPID_PUBLIC_KEY). Generate: npx web-push generate-vapid-keys
VITE_VAPID_PUBLIC_KEY=

# Optional: pre-fill API base for new browser sessions (dev). User can override in Settings.
# VITE_LINK_HELP_API_BASE=http://localhost:8787
