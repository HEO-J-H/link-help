/** Shared by all members when `useHouseholdRegionIncome` is true. */
export interface HouseholdDefaults {
  sido: string;
  sigungu: string;
  incomeBand: string;
  annualIncomeMemoManwon: string;
}
